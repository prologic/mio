#!/usr/bin/env mio

Object for = method(
    i = eval_arg(0)
    n = eval_arg(1)
    s = eval_arg(2)
    m = args at(3)

    if (i < n, m call)

    for (i + s, n, s, m)
)

for (1, 3, 1, "foo" print)
