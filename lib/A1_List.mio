List do (
    sum = method(iterable,
        sum = 0
        iterable foreach(item,
            sum = sum + item
        )
        sum
    )

    map = method(f, iterable,
        map = list
        iterable foreach(item,
            map append(f(item))
        )
        map
    )

    filter = method(f, iterable,
        filter = list
        iterable foreach(item,
            if (f(item), filter append(item))
        )
        filter
    )
)
