# Standard Operators

Object do(
    set("<", method(other, self cmp(other) == -1))
    set("<=", method(other, self cmp(other) < 1))
    set(">", method(other, self cmp(other) == 1))
    set(">=", method(other, self cmp(other) > -1))
    set("is", method(other, self id == (other id)))
)

Number do(
    set("<<", method(other, self * (2 ** other)))
    set(">>", method(other, self / (2 ** other)))
)
