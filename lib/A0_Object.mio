Object do (
    set("", method(arg(0)))
)

True do(
	or = Lobby True
    str = "True"
    not = Lobby False
    else = Lobby True
    elif = Lobby True
    clone = Lobby True
)

False do(
	and = Lobby False
	or = method(v, v bool)
	type = "False"
	str = "False"
	not = Lobby True
    else = method(m, None)
    elif = Lobby Object get("if")
	clone = Lobby False
)

None do(
	not = Lobby True
	type = "None"
	str = "None"
	and = Lobby False
	or = method(v, v bool)
    else = method(m, None)
    elif = Lobby Object get("if")
	clone = Lobby None
)
