World = Object clone do (

    name = "World!"

    init = method(name,
        self name = name ifNone(self name)
    )

    __repr__ = method("World")

    setName = method(name,
        self name = name
    )
)

world = World clone("World!")
