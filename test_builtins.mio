 test_abs = block(
    assert abs(-1) == 1
    assert abs(1) == 1
)


keys foreach(key,
    (key startswith("test")) ifTrue(
        test = get(key)
        try (
            test()
            print(".", end="")
        ) catch (
            print("F", end="")
        )
    )
)
print()
