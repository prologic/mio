print = block(*args, sep=" ", end="\n", file=System stdout,
    args foreach(arg,
        file write(arg) write(sep)
    )
    write(end)
    None
)

True do (
    ifTrue = Object get("evalArg")
)

Number foo = method(
    (self < 2) ifTrue(return "foo")
    "XXX" println
)

Number x = method(
    (self == 1) ifTrue("foo")
)

Number bar = method(
    double = method(n,
        return (n * 2)
    )
    return double(self)
    "XXX" println
)

Number baz = block(
    x = 1
    y = method(return x)
    return y
)
