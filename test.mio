Number foo = method(
    (self < 2) ifTrue(return "foo")
    "XXX" println
)

Number bar = method(
    double = method(n,
        return (n * 2)
    )
    return double(self)
    "XXX" println
)

Number baz = block(
    x = 1
    y = method(return x)
    return y
)
