test = block(**kwargs,
    kwargs
)

complement = block(f,
    block(arg,
        f(arg) bool not
    )
)

odd = block(n, (n % 2) == 1)
even = complement(get("odd"))
    
True do (
    ifTrue = Object get("evalArg")
)

Number foo = method(
    (self < 2) ifTrue(return "foo")
    "XXX" println
)

Number x = method(
    (self == 1) ifTrue("foo")
)

Number bar = method(
    double = method(n,
        return (n * 2)
    )
    return double(self)
    "XXX" println
)

Number baz = block(
    x = 1
    y = method(return x)
    return y
)
