complement = block(f, block(*args, not(f(*args))))

even = block(x, x % 2 == 0)
odd = complement(even)

assert = block(
    expr = call message args at(0)
    (expr eval(call sender)) ifFalse (
        raise AssertionError(expr)
    )
)
